# Hyprland lua

Agents have access to Lua LSP, utilize this for verifying hyprland Lua integrations.

Every `hl.bind` MUST set `description` in its flags table (feeds the
keybindings menu) — including switch binds.
